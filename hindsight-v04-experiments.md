# Эксперименты с Hindsight v0.4.2

**Дата:** 2026-01-30
**Bank ID:** `experiment-v04`
**Mission:** "Изучение предпочтений разработчика в технологиях и инструментах"

---

## 1. Входные данные (Retain)

### Факты, которые мы сохранили:

| # | Текст факта |
|---|-------------|
| 1 | "Пользователь использует Rust для системного программирования и очень доволен производительностью" |
| 2 | "Пользователь считает Python отличным для прототипирования и скриптов, но предпочитает типизированные языки для продакшена" |
| 3 | "Пользователь активно изучает Svelte 5 и считает его намного лучше React из-за простоты и отсутствия virtual DOM" |
| 4 | "Пользователь перешёл с React на Svelte после 5 лет работы с React" |
| 5 | "Пользователь разочаровался в Svelte из-за слабой экосистемы и вернулся на React с RSC (React Server Components)" |

---

## 2. Автоматически извлечённые факты (World Facts)

Система извлекла из входных данных следующие структурированные факты:

```
1. "Пользователь использует язык программирования Rust для системного программирования
    и очень доволен его производительностью"
   Entities: Rust, системное программирование

2. "Пользователь считает Python отличным для прототипирования и скриптов,
    но предпочитает типизированные языки для продакшена"
   Entities: Python, типизированные языки

3. "Пользователь активно изучает Svelte 5 и считает его намного лучше React
    из-за простоты и отсутствия virtual DOM"
   Entities: user, Svelte 5, React

4. "Пользователь работал с React в течение 5 лет"
   Entities: user, React

5. "Пользователь перешёл с React на Svelte"
   Entities: user, Svelte 5, React

6. "Пользователь разочаровался в Svelte из-за слабой экосистемы и перешёл обратно
    к React с использованием React Server Components (RSC)"
   Entities: Пользователь, Svelte 5, React, React Server Components
```

---

## 3. Observations (Автоматическая консолидация)

Система автоматически создала следующие observations из фактов:

### Ключевой observation (эволюция знаний):
```
"User previously considered Svelte 5 superior to React because it is simpler
and lacks a virtual DOM, but later became disappointed with Svelte's weak
ecosystem and switched back to React, now using React Server Components (RSC)."
```

**Важно:** Система сохранила полную историю эволюции мнения — не просто "использует React", а весь путь от Svelte-энтузиазма до возврата на React.

### Другие observations:
- "User uses Rust for systems programming and is very satisfied with its performance"
- "User believes Python is excellent for prototyping and scripting, but prefers typed languages for production"
- "User has 5 years of experience working with React"
- "User finds Svelte's ecosystem to be weak"
- "User uses React Server Components (RSC) in React"

---

## 4. Mental Model

**Name:** "Технологический стек"
**Source Query:** "Какие технологии и языки предпочитает пользователь?"

### Сгенерированный контент:
```
Пользователь отдает предпочтение типизированным языкам для production-кода,
в частности активно использует Rust для системного программирования.
Для быстрых прототипов и скриптов он считает Python отличным выбором,
но в продакшене предпочитает языки с строгой типизацией.

В веб-разработке он имеет многолетний опыт работы с React,
сейчас использует React Server Components.
Было экспериментировано с Svelte, однако из-за слабой экосистемы
Svelte был оставлен в пользу React.
```

---

## 5. Directive (Guardrail)

**Name:** "Язык ответов"
**Content:** "Всегда отвечай на русском языке, даже если вопрос на английском"
**Active:** true

### Тест directive:
- **Вопрос:** "What frontend framework should I use?" (английский)
- **Ответ:** На русском языке (directive сработал!)

---

## 6. Reflect Queries и ответы

### Query 1: "Какой фреймворк для фронтенда лучше рекомендовать этому пользователю?"
```
Лучшим выбором будет Svelte (в частности Svelte 5). Пользователь имеет
пятилетний опыт работы с React, но уже переключился на Svelte и отмечает,
что Svelte 5 гораздо проще, не использует виртуальный DOM и позволяет
быстрее реализовывать интерфейсы...
```
*Примечание: Этот ответ был до добавления факта о возврате на React*

### Query 2 (после добавления противоречащего факта):
После refresh mental model система корректно обновила знания и теперь знает, что пользователь вернулся на React.

---

## 7. Статистика банка

```json
{
  "total_nodes": 13,
  "total_links": 77,
  "total_documents": 5,
  "nodes_by_fact_type": {
    "observation": 7,
    "world": 6
  },
  "links_by_link_type": {
    "semantic": 23,
    "entity": 24,
    "temporal": 30
  }
}
```

---

## 8. Извлечённые сущности (Entities)

- user / Пользователь
- Rust
- Python
- React
- Svelte 5
- React Server Components
- системное программирование
- типизированные языки

---

## Выводы

### Что работает в v0.4:

1. **Observations** — автоматическая консолидация знаний с сохранением истории эволюции
2. **Mental Models** — кэшированные ответы для частых запросов с возможностью refresh
3. **Agentic Reflect** — глубокий синтез информации из нескольких источников
4. **Directives** — жёсткие правила (guardrails) соблюдаются
5. **Mission** — влияет на то, какие знания консолидируются

### Ключевая фича v0.4:
Система не просто хранит последнее состояние, а сохраняет **полную историю эволюции знаний**. Когда мнение пользователя изменилось (Svelte → React), observation отразил весь путь, а не просто заменил старое значение.

---

## 9. Tags — Visibility Scoping

### Назначение
Tags — это НЕ категоризация, а **система видимости** для multi-user agents.

### Эксперимент (bank: tags-demo)
```
Alice: утренние встречи 9:00, VS Code     → tags: ["user:alice"]
Bob: вечерние встречи 14:00, Neovim       → tags: ["user:bob"]
Групповая встреча 12:00                   → tags: ["user:alice", "user:bob"]
```

### Результаты recall:
| Данные | Alice видит | Bob видит | Admin (no tags) |
|--------|-------------|-----------|-----------------|
| Alice 9:00 | ✅ | ❌ | ✅ |
| Bob 14:00 | ❌ | ✅ | ✅ |
| Группа 12:00 | ✅ | ✅ | ✅ |
| VS Code | ✅ | ❌ | ✅ |
| Neovim | ❌ | ✅ | ✅ |

### API (CLI не поддерживает tags!)
```bash
# Retain с тегами
POST /v1/default/banks/{bank}/memories
{"items": [{"content": "...", "tags": ["user:alice"]}]}

# Recall с фильтрацией
POST /v1/default/banks/{bank}/memories/recall
{"query": "...", "tags": ["user:alice"]}
```

### Режимы matching
| Mode | Логика | Untagged |
|------|--------|----------|
| `any` (default) | OR | Включены |
| `any_strict` | OR | Исключены |
| `all` | AND | Включены |
| `all_strict` | AND | Исключены |

---

## 10. Граф памяти — Три типа связей

### Статистика
```json
{
  "total_nodes": 13,
  "total_links": 77,
  "links_by_link_type": {
    "entity": 24,
    "semantic": 23,
    "temporal": 30
  }
}
```

### Entity Links (🟡 золотой, weight: 1.0)
Связывают факты через **общие сущности**:
```
"Svelte 5 лучше React" ←─[React]─→ "Вернулся на React с RSC"
```

### Semantic Links (🩷 розовый, weight: 0.8-0.95)
Связывают факты с **похожим смыслом** (cosine similarity embeddings):
```
"Перешёл с React на Svelte" ←─[0.94]─→ "Разочаровался в Svelte"
```

### Temporal Links (🩵 голубой пунктир, weight: 0.3-1.0)
Связывают факты **близкие по времени**:
```python
weight = max(0.3, 1.0 - (time_diff_hours / 24))
# 1 минута → 0.999
# 12 часов → 0.5
# 24+ часов → 0.3
```

### Пример из эксперимента:
```json
{
  "source": "Svelte 5 лучше React",
  "target": "Перешёл на Svelte",
  "linkType": "temporal",
  "weight": 0.9999  // 4 секунды разницы
}
```

---

## 11. Recall Trace — Graph Traversal

### Структура trace:
```
retrieval_results: 9 стратегий
rrf_merged: 13 результатов после RRF fusion
reranked: 13 после cross-encoder
entry_points: 10 начальных точек графа
visits: 13 посещённых узлов
```

### Стратегии retrieval:
1. **Semantic** — cosine similarity
2. **BM25** — keyword matching
3. **Graph** — traversal по links
4. **MPFP** — Multi-Path Fact Propagation

---

## 12. Multiple Mental Models — Каскадное использование

### Созданные models:
| Name | Query | Content |
|------|-------|---------|
| Frontend Stack | Какой фронтенд? | React с RSC, после Svelte |
| Backend Stack | Языки для бэкенда? | Python + Rust |
| Dev Philosophy | Принципы? | DRY/KISS, типизация |
| Технологический стек | Общее | Синтез всего |

### Каскадность — models ссылаются друг на друга:
```json
{
  "name": "Frontend Stack",
  "based_on": {
    "mental-models": [
      "Backend Stack",
      "Dev Philosophy",
      "Технологический стек"
    ]
  }
}
```

### Два способа использования:

**1. Через Reflect (с reasoning):**
```bash
hindsight memory reflect bank "Помоги выбрать стек"
# → Система находит релевантные models и синтезирует ответ
```

**2. Прямой доступ (instant, NO LLM):**
```bash
hindsight mental-model get bank <id>
# → Мгновенный ответ из БД
```

### Пример Reflect с multiple models:
```
Для нового проекта:

Frontend: React + RSC, TypeScript
Backend: FastAPI (Python) + Rust микросервисы
DevOps: Docker, GitHub Actions

Принципы: DRY/KISS, типизация, тестируемость
```

---

## Архитектурная схема v0.4

```
RETAIN: content → facts → entities → links
            ↓
GRAPH:  [Fact]──entity──[Fact]──temporal──[Fact]
            │ semantic        ↓
            └────────→ [Observation]
            ↓
RECALL: query → multi-strategy → graph expansion → RRF → rerank
            ↓
REFLECT: facts + observations + mental models + directives
            ↓
        synthesized response
```

---

## 13. File Upload — retain-files

### Поддерживаемые типы (CLI)
```
txt, md, json, yaml, yml, toml, xml, csv, log, rst, adoc
```
**НЕ поддерживаются:** .py, .js, .rs и другие исходники (нужно переименовать в .txt)

### Эксперимент
Загрузили 4 файла:
| Файл | Размер | Тип |
|------|--------|-----|
| small.txt | 107B | текст |
| readme.md | 301B | markdown |
| config.json | 114B | JSON |
| http_api.txt | 130KB | Python код (переименован) |

### Результаты
```json
{
  "total_nodes": 221,
  "total_links": 43351,
  "total_documents": 4,
  "nodes_by_fact_type": {
    "world": 154,
    "experience": 48,
    "observation": 19
  }
}
```

**Из 130KB Python файла извлечено:**
- Структура API endpoints
- Описания роутеров
- Связи между компонентами

### Примеры recall:
```
Query: "API endpoints routers FastAPI"
→ "FastAPI app defines GET endpoint at /v1/default/banks/{bank_id}/directives..."

Query: "project settings theme"
→ "test-project 1.0.0 configured with dark theme and Russian language"

Query: "markdown README"
→ "Этот файл создан для тестирования загрузки markdown в Hindsight"
```

### Команда загрузки
```bash
hindsight-embed memory retain-files <bank> <path> --context "description"
hindsight-embed memory retain-files <bank> ./docs/ -r  # рекурсивно
```

---

## 14. Глубокий анализ: Код → Знания

### Что извлечено из 130KB Python кода (http.py)

| Тип | Количество | Примеры |
|-----|------------|---------|
| **World facts** | 149 | Описания endpoints, моделей |
| **Experience facts** | 48 | Паттерны request/response |
| **Observations** | ~60 | Обобщения моделей данных |
| **Entities** | 381 | API paths, models, errors |

### Примеры извлечённых знаний

**World fact (описание):**
```
"The FastAPI app defines a GET endpoint at /v1/default/banks/{bank_id}/directives
that lists directives, returning a DirectiveResponse or 404 if not found."
Entities: [app, DirectiveResponse, bank_id, directive_id]
```

**Experience fact (паттерн поведения):**
```
"api_reflect records a 'reflect' operation metric with the bank ID,
source set to 'api', and the request budget value..."
```

**Observation (обобщение):**
```
"ReflectRequest model defines request fields: query, budget, max_tokens,
include, response_schema (optional), tags, tags_match. When response_schema
is provided, the response includes a structured_output field..."
```

### Reflect: Синтез архитектуры API

Запрос: "Опиши архитектуру API"

Ответ системы (сокращённо):
```
API построен как набор REST-эндпоинтов:

1. Банки (banks)
   - GET /v1/default/banks - список
   - GET .../profile - детали
   - GET .../stats - статистика
   - GET .../graph - граф памяти

2. Память (memories)
   - POST .../memories - запись фактов
   - POST .../memories/recall - семантический поиск
   - GET .../memories/{id} - отдельный факт

3. Директивы, Ментальные модели, Операции...

Обработка ошибок:
- AuthenticationError → 401
- HTTPException → пробрасывается
- ValueError → 400 Bad Request
- Не найдено → 404
- Прочее → 500 + traceback в логах
```

**Использовано 30 observations** для синтеза!

### Кросс-документальные связи

| Документ | Entities | Связи с другими |
|----------|----------|-----------------|
| config.json | 3 | нет (уникальные) |
| readme.md | 8 | нет |
| small.txt | 1 | нет |
| http_api.txt | 381 | внутренние |

Документы слишком разнородны для кросс-связей. Для кросс-документальных связей нужны документы с общими сущностями (например, несколько файлов одного проекта).

---

## 15. Кросс-документальные связи (crossdoc-test)

### Загруженные файлы (один проект)
| Файл | Размер | Entities |
|------|--------|----------|
| models.txt | 13KB | 56 |
| config.txt | 31KB | 113 |
| main.txt | 16KB | 79 |

### Результат: 157 кросс-документальных entities!

**Примеры связей между файлами:**
```
"reranker_provider" → [main.py, config.py]
"log_level" → [main.py, config.py]
"ENV_RERANKER_TEI_MAX_CONCURRENT" → [main.py, config.py]
"mcp_enabled" → [main.py, config.py]
"db_pool_max_size" → [main.py, config.py]
```

### Reflect: Кросс-документальный синтез

**Запрос:** "Как настраивается reranker? Где определяется конфиг и как он используется?"

**Ответ системы (сокращённо):**
```
Настройка reranker происходит через HindsightConfig.
При первом обращении вызывается from_env(), где читаются
переменные окружения с префиксом ENV_RERANKER_*

При инициализации main.py читает конфигурацию,
затем в модуле reranker создаётся провайдер на основе
config.reranker_provider, config.reranker_local_model...

При старте конфигурация выводится через config.log_config()
```

**Система синтезировала знания из ВСЕХ файлов:**
- config.py → определение HindsightConfig, from_env()
- main.py → использование конфига при старте
- Общие entities связали факты из разных файлов

### Как работают кросс-связи

```
config.py                    main.py
    │                           │
    └─── reranker_provider ─────┘
    │           │               │
    └── ENV_RERANKER_* ─────────┘
    │           │               │
    └─── db_pool_max_size ──────┘
             │
      [Entity Links]
             │
    ┌────────┴────────┐
    │   Graph Node    │
    │ (объединяет     │
    │  факты из       │
    │  обоих файлов)  │
    └─────────────────┘
```
